# Set maxclients slightly higher than 10k to handle overhead
maxclients 15000

# KeyDB's flagship multi-threading feature (adjust to your available CPU cores)
server-threads 4

# Prevent idle client buildup (close inactive connections)
timeout 300
tcp-keepalive 60